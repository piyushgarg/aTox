package ltd.evilcorp.core.tox

/** Максимальная длина текстового сообщения (UTF-8) в байтах, разрешенная спецификацией Tox. */
const val MAX_MESSAGE_LENGTH = 1372

/** Максимальный размер аватара пользователя в байтах (64 КБ). */
const val MAX_AVATAR_SIZE = 64 * 1024

/** Стандартная длина HEX-строки адреса Tox ID (76 символов). */
const val TOX_ID_LENGTH = 76
